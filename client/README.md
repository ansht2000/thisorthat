# thisorthat client

The React frontend. Setup, configuration and everything else are in the [main README](../README.md).

```bash
pnpm install
pnpm dev     # http://localhost:5173, with /api proxied to the go server on 8080
pnpm build
pnpm lint
```
